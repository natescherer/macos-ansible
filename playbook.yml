---
- name: Build a new Mac
  hosts: mac
  connection: local
  gather_facts: true

  roles:
    - role: homebrew
      tags: homebrew
    - role: mac_app_store
      tags: mac_app_store
    - role: macos_settings
      tags: macos_settings
